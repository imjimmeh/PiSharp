## Bugs

- [ ] Provider errors don't seem to show; has issue with OPENAI Codex model where context was exceeded, but nothing showed in chat. it just showed working then stopped. no auto compaction either.

## Improvements

- [ ] Add extension support for more UI elements, e g. sidebars
- [ ] Parity with JS version for `/resume` command; filters, search type, etc.
